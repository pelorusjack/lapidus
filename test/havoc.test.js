var assert = require('assert');

describe('Lapidus havoc remediation steps', function () {

    it('reports when a connection is severed', function (done) {
        done();
    });

    it('attempts to reconnect the configured number of times with the configured delay between each attempt', function (done) {
        done();
    });

    it('commits suicide in worker mode when the number of retries is exceeded', function (done) {
        done();
    });

    describe('picks up where it lets off if the backend supports it', function () {
        it('MySQL', function(done) {
            done();
        });

        it('PostgreSQL', function(done) {
            done();
        });

        it('MongoDB', function(done) {
            done();
        });
    });

});